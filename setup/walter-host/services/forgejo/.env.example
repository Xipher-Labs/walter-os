# Generate strong values once (do NOT commit real values):
#   openssl rand -base64 32
#   openssl rand -hex 16
# Save to /opt/walter-vm/services/forgejo/.env on VM (mode 600)
# Mirror in ~/.config/walter-os/secrets.env
