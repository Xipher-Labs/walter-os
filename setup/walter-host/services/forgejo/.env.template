# Forgejo .env — populate via:
#   walter-os secrets-pull   (pulls FORGEJO_DB_PASS from Bitwarden)
# OR fetch from Infisical:
#   infisical secrets get FORGEJO_DB_PASS --domain=https://secrets.${WALTER_DOMAIN} --env=dev

# Postgres password (used by both forgejo + forgejo-db services)
FORGEJO_DB_PASS=
