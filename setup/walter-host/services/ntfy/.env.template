# Copy to .env before starting the optional ntfy profile.

# Example domain for operator docs. Replace the placeholder in server.yml
# manually as shown in README.md; Compose does not template server.yml.
WALTER_DOMAIN=example.com

# Container timezone for log timestamps.
TZ=UTC
