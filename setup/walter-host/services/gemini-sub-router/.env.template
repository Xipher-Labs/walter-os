# gemini-sub-router — environment template
# Set in /opt/walter-vm/services/gemini-sub-router/.env or pass via docker compose --env-file.

# [REQUIRED] API key for requests to this router.
# LiteLLM must supply: Authorization: Bearer <GSR_APIKEY>
# Generate: openssl rand -hex 32
GSR_APIKEY=

# [OPTIONAL] JSON object merged over the default request-alias -> Gemini CLI slug map.
# Example: {"pro":"gemini-2.5-pro"}
MODEL_MAP_JSON=

# Port the router listens on (default: 1458)
PORT=1458
