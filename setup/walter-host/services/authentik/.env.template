# Authentik .env template.
# Copy to .env and populate locally or fetch from Infisical.
# Do not commit populated .env files.

# Generate with:
#   openssl rand -base64 60
AUTHENTIK_SECRET_KEY=

# Database password used by both authentik and the PostgreSQL container.
# Generate with:
#   openssl rand -base64 36
AUTHENTIK_POSTGRESQL__PASSWORD=
