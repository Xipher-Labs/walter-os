# Caddyfile — rendered from Caddyfile.template
#
# DO NOT EDIT THIS FILE DIRECTLY.
# Regenerate: envsubst < setup/caddy/Caddyfile.template > setup/caddy/Caddyfile
# Or: WALTER_DOMAIN=example.com WALTER_ADMIN_EMAIL=admin@example.com \
#     envsubst < setup/caddy/Caddyfile.template > setup/caddy/Caddyfile
#
# This file is a placeholder so the compose mounts don't fail on first clone.
# The bootstrap.sh script renders the real file from the template.

{
  email admin@example.com
}

# Placeholder — replace by running bootstrap.sh or envsubst manually.
:80 {
  respond "Walter-OS Caddy placeholder. Run scripts/bootstrap.sh to configure." 200
}
